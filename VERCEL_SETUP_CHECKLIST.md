# ✅ Чеклист настройки Vercel

## Проверка настроек в Vercel

### ✅ Build Command
```
prisma generate && prisma migrate deploy && next build
```
**Статус:** ✅ Правильно

Это правильно, потому что:
1. `prisma generate` - генерирует Prisma Client
2. `prisma migrate deploy` - применяет миграции к продакшен БД
3. `next build` - собирает Next.js приложение

### ✅ Application Preset
```
Next.js
```
**Статус:** ✅ Правильно

### ✅ Root Directory
```
./
```
**Статус:** ✅ Правильно (корень проекта)

### ✅ Project Name
```
vo-deco-alpha
```
**Статус:** ✅ Хорошо (можно изменить на `vodeco` если хотите)

---

## 📋 Что нужно добавить в Vercel

### 1. Environment Variables (Обязательно!)

В Settings → Environment Variables добавьте:

```env
# Database (Vercel создаст автоматически при добавлении Postgres)
DATABASE_URL=postgresql://... (автоматически)

# JWT Authentication (ОБЯЗАТЕЛЬНО!)
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
JWT_EXPIRES_IN=7d

# Telegram Bot (если используете)
TELEGRAM_BOT_TOKEN=ваш-токен-от-botfather

# App URL (после деплоя)
NEXT_PUBLIC_APP_URL=https://vo-deco-alpha.vercel.app

# Environment
NODE_ENV=production
```

### 2. PostgreSQL Database

1. Vercel Dashboard → Storage → Create Database
2. Выберите **Postgres**
3. Создайте базу данных
4. Vercel автоматически добавит `DATABASE_URL`

### 3. После первого деплоя

Выполните миграции:

```bash
# Локально
npx vercel env pull .env.local
npx prisma migrate deploy

# Или через Vercel CLI
vercel env pull
vercel exec "npx prisma migrate deploy"
```

---

## 🔍 Проверка после деплоя

### 1. Проверьте логи

Vercel Dashboard → Functions → Logs

Должны быть:
- ✅ Успешный build
- ✅ Нет ошибок при старте
- ✅ API routes отвечают

### 2. Проверьте API

Откройте в браузере:
- `https://your-project.vercel.app/api/projects`
- Должен вернуть JSON с проектами

### 3. Проверьте страницы

- `https://your-project.vercel.app/` - главная
- `https://your-project.vercel.app/missions` - миссии
- `https://your-project.vercel.app/library` - библиотека

---

## ⚠️ Частые проблемы

### Ошибка: "DATABASE_URL is not set"
**Решение:** Создайте PostgreSQL в Vercel Storage

### Ошибка: "JWT_SECRET is not set"
**Решение:** Добавьте JWT_SECRET в Environment Variables

### Ошибка миграций
**Решение:** 
```bash
npx prisma migrate deploy
```

### Build ошибка
**Решение:** Проверьте логи в Vercel Dashboard

---

## ✅ Итоговый чеклист

- [ ] Build Command настроен правильно ✅
- [ ] Application Preset: Next.js ✅
- [ ] Root Directory: ./ ✅
- [ ] PostgreSQL база данных создана
- [ ] DATABASE_URL добавлен (автоматически)
- [ ] JWT_SECRET добавлен
- [ ] TELEGRAM_BOT_TOKEN добавлен (если используете)
- [ ] NEXT_PUBLIC_APP_URL добавлен
- [ ] Деплой выполнен успешно
- [ ] Миграции применены
- [ ] Приложение работает

---

## 🎯 Следующие шаги

1. ✅ Настройки Vercel правильные
2. ⏭️ Добавьте Environment Variables
3. ⏭️ Создайте PostgreSQL базу данных
4. ⏭️ Деплой
5. ⏭️ Примените миграции
6. ⏭️ Протестируйте приложение
