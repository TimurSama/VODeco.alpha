# 🔧 Принудительная пересборка в Vercel

## Проблема решена в коде

Файл `app/api/referrals/route.ts` **ПОЛНОСТЬЮ ИСПРАВЛЕН**:
- ✅ Убран `_sum` 
- ✅ Используется `findMany` + `reduce`
- ✅ Коммит `c2bca62` отправлен в репозиторий

## Если Vercel все еще видит ошибку

### Решение 1: Очистка кэша в Vercel Dashboard

1. **Vercel Dashboard** → Ваш проект
2. **Settings** → **General**
3. Прокрутите до **"Build & Development Settings"**
4. Нажмите **"Clear Build Cache"**
5. Передеплойте проект

### Решение 2: Передеплой БЕЗ кэша

1. **Deployments** → последний деплой
2. **"..."** → **"Redeploy"**
3. **ОТКЛЮЧИТЕ** "Use existing Build Cache"
4. Нажмите **"Redeploy"**

### Решение 3: Проверьте коммит

Убедитесь, что Vercel использует правильный коммит:
- Последний коммит: `c2bca62`
- Проверьте в логах деплоя, какой коммит используется

## Что уже сделано

1. ✅ Файл исправлен (нет `_sum`)
2. ✅ Добавлена очистка кэша в `vercel.json`: `rm -rf .next`
3. ✅ Коммит отправлен в GitHub
4. ✅ Все изменения применены

## Проверка

Файл в репозитории на строке 44:
```typescript
select: {  // ✅ Правильно
  amount: true,
},
```

НЕ:
```typescript
_sum: {  // ❌ Старая версия
  amount: true,
},
```

---

**Если ошибка все еще есть после очистки кэша - это проблема кэша Vercel, не кода!**
