# 📝 Пошаговая инструкция по настройке

## Что нужно для следующих шагов:

### ✅ 1. Запустить миграции: `npx prisma migrate dev`

**Что это делает:**
- Создает структуру базы данных согласно схеме Prisma
- Создает файлы миграций в `prisma/migrations/`
- Применяет миграции к базе данных

**Что нужно:**
- ✅ Файл `.env.local` с `DATABASE_URL`
- ✅ Установленные зависимости (`npm install`)

**Команда:**
```bash
npx prisma migrate dev --name init
```

---

### ✅ 2. Запустить seed: `npm run db:seed`

**Что это делает:**
- Заполняет базу данных тестовыми данными
- Создает тестового пользователя, проекты, новости, миссии

**Что нужно:**
- ✅ Миграции выполнены (шаг 1)
- ✅ База данных создана

**Команда:**
```bash
npm run db:seed
```

---

### ✅ 3. Настроить переменные окружения (JWT_SECRET)

**Что нужно создать:**

1. **Создайте файл `.env.local`** (скопируйте из `.env.example`):
```bash
cp .env.example .env.local
```

2. **Откройте `.env.local` и настройте:**

```env
# База данных (для локальной разработки - SQLite)
DATABASE_URL="file:./dev.db"

# JWT секретный ключ (ОБЯЗАТЕЛЬНО измените!)
# Пример сгенерированного ключа:
JWT_SECRET="cedb0e61cc7bfa763b9beaf567f2096af8688935928ec1ca3b35d9e40aed2eee"
JWT_EXPIRES_IN="7d"

# Telegram Bot (если используете Telegram auth)
TELEGRAM_BOT_TOKEN="ваш-токен-бота"

# URL приложения
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# News API (опционально)
NEXT_PUBLIC_NEWS_API_KEY="ваш-ключ"
```

**⚠️ ВАЖНО:** 
- `JWT_SECRET` должен быть минимум 32 символа
- Используйте случайный ключ (можно сгенерировать: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)
- **НЕ коммитьте** `.env.local` в Git!

---

### ✅ 4. Протестировать функции

**Что нужно проверить:**

1. **Запустите приложение:**
```bash
npm run dev
```

2. **Откройте в браузере:** http://localhost:3000

3. **Проверьте основные функции:**
   - ✅ Регистрация через Telegram
   - ✅ Вход в систему
   - ✅ Просмотр профиля
   - ✅ Просмотр миссий
   - ✅ Просмотр библиотеки
   - ✅ Просмотр новостей
   - ✅ Кошелек и стейкинг

---

## 🌐 GitHub + Vercel - Подходит ли?

### ✅ **ДА, отлично подходит!**

**Преимущества:**
- 🚀 Быстрый деплой (5-10 минут)
- 💰 Бесплатный тариф для старта
- 🔄 Автоматические деплои при push в GitHub
- 📊 Встроенная аналитика
- 🔒 Автоматический HTTPS
- 💾 Встроенная PostgreSQL база данных

**Ограничения (на бесплатном тариле):**
- 100GB bandwidth/месяц
- Ограничения на время выполнения функций (10 сек на Hobby)
- Но для MVP этого более чем достаточно!

---

## 🔄 Переезд на свой сервер (когда будете готовы)

### Когда стоит переезжать:
- 📈 Когда нужен больший контроль
- 💰 Когда свой сервер дешевле (при масштабировании)
- 🔧 Когда нужны кастомные настройки

### Как переехать:
1. **Экспортируйте данные** из Vercel PostgreSQL
2. **Настройте свой сервер** (VPS/Docker)
3. **Импортируйте данные**
4. **Измените DATABASE_URL**
5. **Деплой кода**

**Подробные инструкции в `DEPLOYMENT_GUIDE.md`**

---

## 📋 Полный чеклист настройки

### Локально:
- [ ] `npm install` выполнен
- [ ] `.env.local` создан и настроен
- [ ] `JWT_SECRET` сгенерирован (минимум 32 символа)
- [ ] `npx prisma migrate dev` выполнен
- [ ] `npm run db:seed` выполнен
- [ ] `npm run dev` запущен
- [ ] Приложение работает на http://localhost:3000

### На Vercel:
- [ ] Код загружен на GitHub
- [ ] Проект создан в Vercel
- [ ] PostgreSQL база данных создана в Vercel Storage
- [ ] Все переменные окружения добавлены в Vercel
- [ ] Деплой выполнен успешно
- [ ] Миграции применены (`npx prisma migrate deploy`)
- [ ] Приложение работает на вашем домене

---

## 🆘 Частые проблемы

### Ошибка: "DATABASE_URL is not set"
**Решение:** Создайте `.env.local` с `DATABASE_URL="file:./dev.db"`

### Ошибка: "JWT_SECRET is not set"
**Решение:** Добавьте `JWT_SECRET` в `.env.local`

### Ошибка миграций
**Решение:** 
```bash
npx prisma migrate reset  # ОСТОРОЖНО: удалит данные
npx prisma migrate dev --name init
```

### Build ошибка на Vercel
**Решение:** 
- Проверьте логи в Vercel Dashboard
- Убедитесь, что все переменные окружения добавлены
- Проверьте, что PostgreSQL создан

---

## 📚 Дополнительная документация

- `README_DEPLOY.md` - краткая инструкция
- `QUICK_START.md` - быстрый старт
- `DEPLOYMENT_GUIDE.md` - подробное руководство
- `docs/IMPLEMENTATION_SUMMARY.md` - что реализовано
