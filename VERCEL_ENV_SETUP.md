# 🔐 Настройка Environment Variables в Vercel

## 📋 Какие переменные нужно добавить

### ✅ Обязательные переменные

1. **JWT_SECRET** - секретный ключ для JWT токенов
2. **JWT_EXPIRES_IN** - срок действия токенов
3. **DATABASE_URL** - автоматически создается при добавлении PostgreSQL
4. **NEXT_PUBLIC_APP_URL** - URL вашего приложения

### ⚙️ Опциональные переменные

5. **TELEGRAM_BOT_TOKEN** - если используете Telegram auth
6. **NEWS_API_KEY** - если используете NewsAPI
7. **NODE_ENV** - окружение (production)

---

## 🚀 Пошаговая инструкция

### Шаг 1: Откройте настройки проекта в Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите в свой аккаунт
3. Выберите проект `vo-deco-alpha` (или ваш проект)
4. Перейдите в **Settings** (вверху справа)
5. Выберите **Environment Variables** в меню слева

### Шаг 2: Добавьте переменные

Нажмите **"Add New"** и добавьте каждую переменную:

#### 1. JWT_SECRET (ОБЯЗАТЕЛЬНО!)

**Key:** `JWT_SECRET`

**Value:** Сгенерируйте случайный ключ (минимум 32 символа)

Пример сгенерированного ключа:
```
cedb0e61cc7bfa763b9beaf567f2096af8688935928ec1ca3b35d9e40aed2eee
```

**Как сгенерировать:**
```bash
# В терминале
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Environment:** Выберите все (Production, Preview, Development)

**Важно:** Это критически важная переменная! Не делитесь ею ни с кем!

---

#### 2. JWT_EXPIRES_IN

**Key:** `JWT_EXPIRES_IN`

**Value:** `7d`

**Environment:** Все (Production, Preview, Development)

**Описание:** Срок действия JWT токенов (7 дней)

---

#### 3. NEXT_PUBLIC_APP_URL

**Key:** `NEXT_PUBLIC_APP_URL`

**Value:** `https://vo-deco-alpha.vercel.app` (или ваш домен)

**Как узнать URL:**
- После первого деплоя Vercel покажет URL
- Или в Settings → Domains

**Environment:** Все

**Важно:** Используйте `NEXT_PUBLIC_` префикс для переменных, доступных в браузере

---

#### 4. TELEGRAM_BOT_TOKEN (если используете Telegram)

**Key:** `TELEGRAM_BOT_TOKEN`

**Value:** Ваш токен от @BotFather

**Как получить:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` или `/mybots`
3. Выберите вашего бота
4. Скопируйте токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Environment:** Все

---

#### 5. NEWS_API_KEY (опционально)

**Key:** `NEWS_API_KEY`

**Value:** Ваш ключ от NewsAPI (серверная переменная)

**Как получить:**
1. Зарегистрируйтесь на [newsapi.org](https://newsapi.org)
2. Получите бесплатный API ключ
3. Скопируйте ключ

**Environment:** Все

---

#### 6. NODE_ENV

**Key:** `NODE_ENV`

**Value:** `production`

**Environment:** Production (только для продакшена)

---

#### 7. DATABASE_URL (автоматически)

**Key:** `DATABASE_URL`

**Value:** Автоматически создается при добавлении PostgreSQL

**Как создать:**
1. В Vercel Dashboard → **Storage** → **Create Database**
2. Выберите **Postgres**
3. Создайте базу данных
4. Vercel автоматически добавит `DATABASE_URL` в Environment Variables

---

## 📝 Итоговый список переменных

После настройки у вас должно быть:

```
✅ DATABASE_URL=postgresql://... (автоматически)
✅ JWT_SECRET=ваш-секретный-ключ
✅ JWT_EXPIRES_IN=7d
✅ NEXT_PUBLIC_APP_URL=https://vo-deco-alpha.vercel.app
✅ TELEGRAM_BOT_TOKEN=ваш-токен (если используете)
✅ NEWS_API_KEY=ваш-ключ (опционально)
✅ NODE_ENV=production (только Production)
```

---

## 🔄 После добавления переменных

### 1. Передеплойте проект

После добавления переменных нужно передеплоить:

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** → **"Redeploy"**
4. Или сделайте новый commit и push в GitHub (автоматический деплой)

### 2. Проверьте логи

После деплоя проверьте логи:

1. Vercel Dashboard → **Functions** → **Logs**
2. Убедитесь, что нет ошибок
3. Проверьте, что переменные загружены

---

## ✅ Проверка правильности настройки

### Тест 1: Проверка API

Откройте в браузере:
```
https://your-project.vercel.app/api/projects
```

Должен вернуть JSON (не ошибку).

### Тест 2: Проверка аутентификации

Попробуйте зарегистрироваться через Telegram (если настроен).

### Тест 3: Проверка базы данных

После деплоя выполните миграции:
```bash
npx vercel env pull .env.local
npx prisma migrate deploy
```

---

## ⚠️ Важные моменты

### Безопасность

1. **Никогда не коммитьте** `.env` файлы в Git
2. **Не делитесь** JWT_SECRET ни с кем
3. **Используйте разные** JWT_SECRET для Production и Development

### Префиксы

- `NEXT_PUBLIC_` - переменные доступны в браузере (публичные)
- Без префикса - только на сервере (секретные)

### Environment

- **Production** - только для продакшена
- **Preview** - для preview деплоев (pull requests)
- **Development** - для локальной разработки

---

## 🆘 Проблемы и решения

### Ошибка: "JWT_SECRET is not set"

**Решение:** Добавьте JWT_SECRET в Environment Variables

### Ошибка: "DATABASE_URL is not set"

**Решение:** Создайте PostgreSQL базу данных в Vercel Storage

### Переменные не применяются

**Решение:** 
1. Передеплойте проект
2. Проверьте, что выбрали правильный Environment (Production/Preview/Development)

### Переменные не видны в коде

**Решение:**
- Для браузера используйте `NEXT_PUBLIC_` префикс
- Перезапустите dev сервер после изменения `.env.local`

---

## 📚 Дополнительная информация

- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
- [Next.js Environment Variables](https://nextjs.org/docs/app/building-your-application/configuring/environment-variables)
