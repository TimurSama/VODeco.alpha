# 🚀 Инструкция по развертыванию VODeco MVP

## ✅ GitHub + Vercel - Идеально для старта!

**Да, GitHub + Vercel отлично подойдет для начала!** Это самый быстрый и простой способ запустить приложение. Позже можно легко переехать на свой сервер.

---

## 📋 Что нужно сделать сейчас:

### 1️⃣ Локальная настройка (5 минут)

1. Создайте `.env.local` по списку из `ENV_VARIABLES_LIST.md` и укажите:
   - `JWT_SECRET` (сгенерируйте случайный ключ минимум 32 символа)
   - `TELEGRAM_BOT_TOKEN` (если используете)
   - `NEXT_PUBLIC_APP_URL=http://localhost:3000`
2. Запустите миграции: `npx prisma migrate dev --name init`
3. Заполните тестовыми данными: `npm run db:seed`
4. Запустите локально: `npm run dev`

### 2️⃣ Деплой на Vercel (10 минут)

1. **Загрузите на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: VODeco MVP"
   git remote add origin https://github.com/ваш-username/vod-eco-mvp.git
   git push -u origin main
   ```

2. **Подключите к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Выберите ваш GitHub репозиторий
   - Vercel автоматически определит Next.js

3. **Добавьте переменные окружения в Vercel:**
   - Settings → Environment Variables
   - Добавьте все из `ENV_VARIABLES_LIST.md`:
     ```
     JWT_SECRET=ваш-секретный-ключ
     JWT_EXPIRES_IN=7d
     TELEGRAM_BOT_TOKEN=ваш-токен
     NEXT_PUBLIC_APP_URL=https://ваш-проект.vercel.app
     ```

4. **Создайте PostgreSQL базу данных:**
   - В Vercel Dashboard → Storage → Create Database
   - Выберите **Postgres**
   - Vercel автоматически добавит `DATABASE_URL`

5. **Деплой:**
   - Нажмите "Deploy"
   - После деплоя выполните миграции:
   ```bash
   npx vercel env pull .env.local
   npx prisma migrate deploy
   npm run db:seed  # опционально, для тестовых данных
   ```

---

## 🔄 Переезд на свой сервер (когда будете готовы)

### Простой способ:
1. Экспортируйте данные из Vercel PostgreSQL
2. Настройте свой сервер (VPS/Docker)
3. Импортируйте данные
4. Измените `DATABASE_URL`
5. Деплой кода

**Подробные инструкции в `DEPLOYMENT_GUIDE.md`**

---

## ⚡ Быстрый старт (одна команда)

```bash
# Локально
npm install && npx prisma migrate dev && npm run db:seed && npm run dev
```

---

## 📝 Важные моменты

✅ **GitHub + Vercel** - отлично для MVP и начала работы  
✅ **Бесплатный тариф** Vercel достаточен для старта  
✅ **Легкая миграция** на свой сервер в будущем  
✅ **Автоматические деплои** при каждом push в GitHub  

---

## 🆘 Проблемы?

- **Ошибка миграций?** → Проверьте `DATABASE_URL`
- **Ошибка build?** → Смотрите логи в Vercel Dashboard
- **База данных не работает?** → Убедитесь, что PostgreSQL создан в Vercel Storage

---

## 📚 Дополнительная документация

- `QUICK_START.md` - быстрый старт
- `DEPLOYMENT_GUIDE.md` - подробное руководство по деплою
- `docs/IMPLEMENTATION_SUMMARY.md` - что реализовано
