# 📝 Список Environment Variables для Vercel

## ✅ Обязательные переменные (минимум для работы)

### 1. JWT_SECRET
```
Key: JWT_SECRET
Value: ba0a89f5cdb813af31576f3889c601e6e435922c911f157faa58076e04294e51
Environment: All (Production, Preview, Development)
```
**⚠️ ВАЖНО:** Используйте этот ключ или сгенерируйте свой (минимум 32 символа)

### 2. JWT_EXPIRES_IN
```
Key: JWT_EXPIRES_IN
Value: 7d
Environment: All
```

### 3. NEXT_PUBLIC_APP_URL
```
Key: NEXT_PUBLIC_APP_URL
Value: https://vo-deco-alpha.vercel.app
Environment: All
```
**Примечание:** Замените на ваш реальный URL после первого деплоя

### 4. DATABASE_URL
```
Key: DATABASE_URL
Value: [автоматически создается при добавлении PostgreSQL]
Environment: All
```
**Как получить:** Vercel Dashboard → Storage → Create Database → Postgres

---

## ⚙️ Опциональные переменные

### 5. TELEGRAM_BOT_TOKEN
```
Key: TELEGRAM_BOT_TOKEN
Value: [ваш токен от @BotFather]
Environment: All
```
**Формат:** `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### 6. NEXT_PUBLIC_NEWS_API_KEY
```
Key: NEXT_PUBLIC_NEWS_API_KEY
Value: [ваш ключ от newsapi.org]
Environment: All
```
**Опционально:** Только если используете NewsAPI

### 7. NODE_ENV
```
Key: NODE_ENV
Value: production
Environment: Production only
```

---

## 🎯 Быстрая копия для Vercel

Скопируйте и вставьте в Vercel Environment Variables:

| Key | Value | Environment |
|-----|-------|-------------|
| `JWT_SECRET` | `ba0a89f5cdb813af31576f3889c601e6e435922c911f157faa58076e04294e51` | All |
| `JWT_EXPIRES_IN` | `7d` | All |
| `NEXT_PUBLIC_APP_URL` | `https://vo-deco-alpha.vercel.app` | All |
| `DATABASE_URL` | *(автоматически)* | All |
| `TELEGRAM_BOT_TOKEN` | *(ваш токен)* | All |
| `NODE_ENV` | `production` | Production |

---

## 📍 Где добавить в Vercel

### Пошаговая инструкция:

1. **Откройте Vercel Dashboard**
   - Зайдите на [vercel.com](https://vercel.com)
   - Войдите в аккаунт

2. **Выберите проект**
   - Найдите проект `vo-deco-alpha`
   - Кликните на него

3. **Откройте Settings**
   - Вверху справа нажмите **"Settings"**
   - В меню слева выберите **"Environment Variables"**

4. **Добавьте переменные**
   - Нажмите кнопку **"Add New"**
   - Введите **Key** (например, `JWT_SECRET`)
   - Введите **Value** (например, `ba0a89f5cdb813af31576f3889c601e6e435922c911f157faa58076e04294e51`)
   - Выберите **Environment** (лучше выбрать "All" для всех)
   - Нажмите **"Save"**

5. **Повторите для каждой переменной**

6. **Создайте PostgreSQL базу данных**
   - В Vercel Dashboard → **Storage** → **Create Database**
   - Выберите **Postgres**
   - Создайте базу данных
   - Vercel автоматически добавит `DATABASE_URL`

7. **Передеплойте проект**
   - Deployments → последний деплой → "..." → "Redeploy"
   - Или сделайте новый commit в GitHub

---

## ✅ Проверка

После добавления всех переменных и передеплоя:

1. **Проверьте логи:**
   - Vercel Dashboard → Functions → Logs
   - Не должно быть ошибок про отсутствующие переменные

2. **Проверьте API:**
   - Откройте `https://your-project.vercel.app/api/projects`
   - Должен вернуть JSON

3. **Примените миграции:**
   ```bash
   npx vercel env pull .env.local
   npx prisma migrate deploy
   ```

---

## 🔒 Безопасность

- ✅ **НЕ коммитьте** `.env` файлы в Git
- ✅ **НЕ делитесь** JWT_SECRET
- ✅ Используйте **разные ключи** для Production и Development
- ✅ Переменные с `NEXT_PUBLIC_` доступны в браузере (публичные)
- ✅ Переменные без префикса только на сервере (секретные)
