# 🚀 Инструкция по деплою VODeco MVP

## Подготовка к деплою

### 1. Настройка базы данных

Для продакшена требуется PostgreSQL. Рекомендуемые провайдеры:
- **Vercel Postgres** (интеграция с Vercel)
- **Neon** (serverless PostgreSQL)
- **Supabase** (PostgreSQL с дополнительными функциями)
- **Railway** (простой деплой PostgreSQL)

#### Настройка Prisma для продакшена

1. Обновите `prisma/schema.prisma`:
```prisma
datasource db {
  provider = "postgresql"  // Измените с sqlite на postgresql
  url      = env("DATABASE_URL")
}
```

2. Создайте миграцию:
```bash
npm run db:migrate
```

### 2. Переменные окружения

Создайте файл `.env.example` (уже создан) и настройте переменные в Vercel:

**Обязательные:**
- `DATABASE_URL` - URL PostgreSQL базы данных
- `NEXT_PUBLIC_APP_URL` - URL вашего приложения (например, `https://vodeco.vercel.app`)

**Опциональные:**
- `NEXT_PUBLIC_NEWS_API_KEY` - для расширенных новостных функций
- `NEXT_PUBLIC_TELEGRAM_BOT_TOKEN` - для Telegram Mini App
- `JWT_SECRET` - для аутентификации (если используется)
- `ENCRYPTION_KEY` - для шифрования данных (если используется)

## Деплой на Vercel

### Вариант 1: Автоматический деплой через GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/yourusername/vod-eco-mvp.git
   git push -u origin main
   ```

2. **Подключите к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub
   - Нажмите "Add New Project"
   - Выберите репозиторий `vod-eco-mvp`
   - Нажмите "Deploy"

3. **Настройте переменные окружения:**
   - В Vercel Dashboard → Settings → Environment Variables
   - Добавьте все необходимые переменные из `.env.example`
   - Убедитесь, что `DATABASE_URL` указывает на PostgreSQL

4. **Настройте базу данных:**
   - После первого деплоя выполните миграции:
   ```bash
   npm run db:migrate:deploy
   ```
   - Или используйте Vercel CLI:
   ```bash
   vercel env pull .env.local
   npm run db:migrate:deploy
   ```

5. **Заполните базу данных:**
   ```bash
   npm run db:seed
   ```

### Вариант 2: Деплой через Vercel CLI

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт:**
   ```bash
   vercel login
   ```

3. **Задеплойте проект:**
   ```bash
   vercel --prod
   ```

4. **Настройте переменные окружения:**
   ```bash
   vercel env add DATABASE_URL
   vercel env add NEXT_PUBLIC_APP_URL
   # и т.д.
   ```

## Настройка базы данных на Vercel

### Использование Vercel Postgres

1. В Vercel Dashboard → Storage → Create Database → Postgres
2. Скопируйте `DATABASE_URL` из настроек базы данных
3. Добавьте в Environment Variables
4. Обновите `prisma/schema.prisma` для использования PostgreSQL
5. Выполните миграции:
   ```bash
   vercel env pull .env.local
   npm run db:migrate:deploy
   npm run db:seed
   ```

### Использование внешней базы данных (Neon, Supabase, Railway)

1. Создайте базу данных на выбранном провайдере
2. Скопируйте connection string
3. Добавьте в Vercel Environment Variables как `DATABASE_URL`
4. Выполните миграции (см. выше)

## Проверка деплоя

После успешного деплоя проверьте:

- ✅ Production URL работает (например, `https://vodeco.vercel.app`)
- ✅ API routes отвечают (`/api/projects`, `/api/news`)
- ✅ База данных подключена (проверьте через `/api/water-resources`)
- ✅ 3D Globe загружается на `/dashboard`
- ✅ Все страницы доступны и работают

## Автоматический деплой

После настройки, каждый push в `main` будет автоматически деплоиться:

- **Push в `main`** → Production деплой
- **Pull Request** → Preview деплой (уникальный URL)
- **Автоматическая сборка** при каждом коммите

## Troubleshooting

### Ошибка: "Prisma Client not generated"
```bash
# Решение: добавьте postinstall скрипт в package.json (уже добавлен)
npm run postinstall
```

### Ошибка: "Database connection failed"
- Проверьте `DATABASE_URL` в Environment Variables
- Убедитесь, что база данных доступна из интернета
- Проверьте firewall настройки базы данных

### Ошибка: "Build failed"
- Проверьте логи сборки в Vercel Dashboard
- Убедитесь, что все зависимости установлены
- Проверьте, что `prisma generate` выполняется в `postinstall`

### Ошибка: "Module not found"
- Убедитесь, что все зависимости в `package.json`
- Проверьте, что импорты используют правильные пути
- Очистите `.next` и пересоберите: `rm -rf .next && npm run build`

## Дополнительные настройки

### Кастомный домен

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

### Мониторинг и аналитика

- **Vercel Analytics**: Включите в Dashboard → Analytics
- **Sentry**: Добавьте для отслеживания ошибок
- **LogRocket**: Для мониторинга производительности

## Поддержка

При возникновении проблем:
1. Проверьте логи в Vercel Dashboard
2. Проверьте GitHub Issues
3. Создайте новый Issue с описанием проблемы
