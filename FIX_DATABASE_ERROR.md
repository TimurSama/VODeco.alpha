# 🔧 Исправление ошибки DATABASE_URL

## ❌ Проблема

```
Error: Environment variable not found: DATABASE_URL
```

Это происходит потому что:
1. PostgreSQL база данных еще не создана в Vercel
2. Переменная `DATABASE_URL` не добавлена в Environment Variables

## ✅ Решение (2 варианта)

### Вариант 1: Создать PostgreSQL СЕЙЧАС (Рекомендуется)

#### Шаг 1: Создайте PostgreSQL базу данных

1. **Откройте Vercel Dashboard**
   - Зайдите на [vercel.com](https://vercel.com)
   - Выберите проект `vo-deco-alpha`

2. **Создайте базу данных**
   - В меню слева нажмите **"Storage"**
   - Нажмите **"Create Database"**
   - Выберите **"Postgres"**
   - Нажмите **"Create"**
   - Дождитесь создания (30-60 секунд)

3. **Vercel автоматически добавит DATABASE_URL**
   - После создания Vercel автоматически добавит `DATABASE_URL` в Environment Variables
   - Проверьте: Settings → Environment Variables

#### Шаг 2: Передеплойте проект

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой (который упал)
3. Нажмите **"..."** → **"Redeploy"**
4. Сборка должна пройти успешно

---

### Вариант 2: Временно изменить Build Command (если база данных еще не готова)

Если вы еще не готовы создать базу данных, можно временно изменить build command:

**Измените `vercel.json`:**

```json
{
  "buildCommand": "prisma generate && next build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "false"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
```

**Изменение:** Убрали `prisma migrate deploy` из build command

**После создания базы данных:**
1. Верните `prisma migrate deploy` в build command
2. Выполните миграции вручную:
   ```bash
   npx vercel env pull .env.local
   npx prisma migrate deploy
   ```

---

## 🎯 Рекомендуемый порядок действий

### 1. Создайте PostgreSQL (СЕЙЧАС)

1. Vercel Dashboard → **Storage** → **Create Database** → **Postgres**
2. Дождитесь создания
3. Vercel автоматически добавит `DATABASE_URL`

### 2. Добавьте остальные переменные

Если еще не добавили:
- `JWT_SECRET`
- `JWT_EXPIRES_IN`
- `NEXT_PUBLIC_APP_URL`

### 3. Передеплойте

1. Deployments → последний деплой → **"..."** → **"Redeploy"**
2. Сборка должна пройти успешно

### 4. После успешного деплоя

Примените миграции (если нужно):

```bash
# Локально
npx vercel env pull .env.local
npx prisma migrate deploy
```

Или через Vercel CLI:
```bash
vercel env pull
vercel exec "npx prisma migrate deploy"
```

---

## ⚠️ Важно

**Для продакшена обязательно нужна PostgreSQL база данных!**

SQLite (`file:./dev.db`) работает только локально, для Vercel нужен PostgreSQL.

---

## 📝 Чеклист

- [ ] PostgreSQL база данных создана в Vercel Storage
- [ ] DATABASE_URL автоматически добавлен (Vercel делает это)
- [ ] JWT_SECRET добавлен в Environment Variables
- [ ] JWT_EXPIRES_IN добавлен
- [ ] NEXT_PUBLIC_APP_URL добавлен
- [ ] Проект передеплоен
- [ ] Сборка прошла успешно
- [ ] Миграции применены (после деплоя)

---

## 🆘 Если все еще не работает

1. **Проверьте Environment Variables:**
   - Settings → Environment Variables
   - Убедитесь, что `DATABASE_URL` есть
   - Убедитесь, что выбран Environment "All"

2. **Проверьте логи:**
   - Functions → Logs
   - Ищите ошибки

3. **Попробуйте пересоздать базу данных:**
   - Storage → удалите старую → создайте новую
