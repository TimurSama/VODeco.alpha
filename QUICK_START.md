# Быстрый старт VODeco MVP

## 🚀 Локальная разработка

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка переменных окружения
Создайте `.env.local` по списку из `ENV_VARIABLES_LIST.md` и укажите:
- `JWT_SECRET` (минимум 32 символа, случайный)
- `TELEGRAM_BOT_TOKEN` (если используете Telegram auth)
- `NEXT_PUBLIC_APP_URL` (`http://localhost:3000` для локальной разработки)

### 3. Инициализация базы данных
```bash
# Создание миграций
npx prisma migrate dev --name init

# Заполнение тестовыми данными
npm run db:seed
```

### 4. Запуск приложения
```bash
npm run dev
```

Приложение будет доступно на http://localhost:3000

---

## 🌐 Деплой на Vercel

### Быстрый деплой (5 минут)

1. **Загрузите код на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/vod-eco-mvp.git
   git push -u origin main
   ```

2. **Подключите к Vercel**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Выберите ваш GitHub репозиторий
   - Vercel автоматически определит Next.js

3. **Добавьте переменные окружения в Vercel**
   - Settings → Environment Variables
   - Добавьте все переменные из `ENV_VARIABLES_LIST.md`
   - **Важно**: Создайте PostgreSQL базу данных в Vercel Storage

4. **Деплой**
   - Нажмите "Deploy"
   - После деплоя выполните миграции:
   ```bash
   npx vercel env pull .env.local
   npx prisma migrate deploy
   ```

---

## ✅ Чеклист перед деплоем

- [ ] Код загружен на GitHub
- [ ] `.env` файлы в `.gitignore`
- [ ] Все переменные окружения настроены в Vercel
- [ ] PostgreSQL база данных создана в Vercel
- [ ] Миграции выполнены
- [ ] Приложение работает локально

---

## 📝 Важные переменные окружения

```env
# Обязательные
DATABASE_URL=postgresql://... (Vercel создаст автоматически)
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app

# Опциональные
TELEGRAM_BOT_TOKEN=ваш-токен-бота
NEWS_API_KEY=ваш-ключ-newsapi
```

---

## 🔄 Миграция на собственный сервер (когда будете готовы)

1. **Экспорт данных из Vercel PostgreSQL**
2. **Импорт на новый сервер**
3. **Изменение DATABASE_URL**
4. **Деплой кода на сервер**

Подробные инструкции в `DEPLOYMENT_GUIDE.md`
